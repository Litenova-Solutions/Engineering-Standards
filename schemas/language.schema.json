{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Litenova-Solutions/Engineering-Standards/schemas/language.schema.json",
  "title": "Litenova Project Language Record",
  "description": "The closed vocabulary a project's prose is checked against: each term, the synonyms rejected in favour of it, the scope each rejection binds, and the mannered terms no page may use.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "terms", "mannered"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "terms": {
      "description": "One entry per concept. The glossary defines the meaning; this record is what the validator reads.",
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/term" }
    },
    "mannered": {
      "description": "Terms that substitute metaphor or flourish for direct statement. Rejected everywhere, with no scope.",
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/mannered" }
    }
  },
  "$defs": {
    "term": {
      "type": "object",
      "additionalProperties": false,
      "required": ["term", "rejected"],
      "properties": {
        "term": {
          "description": "The one word or phrase this project uses for the concept.",
          "type": "string",
          "minLength": 1
        },
        "rejected": {
          "description": "Words that name the same concept and must not appear inside the scope.",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "scope": {
          "description": "A regular expression over the documentation-root-relative path, with forward slashes. Absent means every page. A word correct in one module and wrong in another carries the scope of the module that rejects it.",
          "type": "string",
          "minLength": 1
        },
        "reason": {
          "description": "Why the synonym is rejected here. Appears in the diagnostic, so a reader fixes the sentence rather than searching for the rule.",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "mannered": {
      "type": "object",
      "additionalProperties": false,
      "required": ["term", "instead"],
      "properties": {
        "term": {
          "description": "The metaphor or stock phrase.",
          "type": "string",
          "minLength": 1
        },
        "instead": {
          "description": "The literal phrase that carries the same meaning. Appears in the diagnostic as the replacement to reach for.",
          "type": "string",
          "minLength": 1
        }
      }
    }
  }
}
