# GitHub Copilot Instructions

Read `AGENTS.md` as the canonical agent contract. In a consumer repository, read `standards/AGENTS.md`, `standards.project.json`, the matching manifest load plan, the active use case, and only the enabled recipes.
