# Changelog

## v1.19.0

- Added `CORE.SYSTEM.MAPPING.001` and `CORE.SYSTEM.MAPPING.002`, which resolve the names a use-case specification points its implementation at. The Implementation mapping is the only part of a page that names code, and nothing in the page failed when that code moved. A renamed handler left a page that read correctly and named an artifact that no longer existed, which is worse than a page carrying no mapping at all, because a reader trusts the one that reads well. The first provision requires every code name in the table to resolve to a declared type, a member of one, a project, or an existing path. A route carries a path rather than an identifier, so the entry-point checks own it, and a row that names no artifact writes `None`. The second requires an `implemented` or `verified` page to name the handler its own operation folder declares, which is the half of the relation `CORE.SYSTEM.COVERAGE.001` could never see: parity proves a handler and a page exist for each other, and not that the page names that handler.
- Added `CORE.SYSTEM.CONSUMERS.001` and `CORE.SYSTEM.CONSUMERS.002`, which close the edge between a use case and the surfaces that call it. A page declared the use cases it called, so the edge ran one way, and nothing answered the question asked before every change: who breaks if this operation moves. A command-line tool, a distributed server, an event reaction and a scheduled run sat outside the graph entirely. The first provision requires an implemented use case to name every declared surface that invokes it, or to write `None` with the reason, because a use case reached only by a schedule has a real answer and writing it down separates it from one nobody wired up. The second requires a use case a page declares to name that page back.
- Added `paths.surfaces` to `schemas/standards-project.schema.json`, so consumer topology arrives as configuration and the linkage rules name no project. A frontend is already a surface under its own name, and the new field names the rest.
- Replaced `BACKEND.TESTING.TRACE.001` with `BACKEND.TESTING.TRACE.002`, which names one citation form: `[Trait("AcceptanceCriterion", "<identifier>")]`. The retired rule asked for a recognized reference and named no form, so a static scan had to guess which string in a test file was a citation. A comment, a variable name and a skipped test each carried the identifier and none of them proved anything.
- Replaced `FRONTEND.TESTING.TRACE.001` with `FRONTEND.TESTING.TRACE.002`, which puts the identifier at the start of the test title in square brackets. The retired rule accepted a title or metadata, which is two places and no stated form. A runner prints the title, so a reader watching a failure sees the criterion without opening the file.
- Made the acceptance trace real in `tools/validate-consumer.mjs`. Two verification rows had said the validator resolved each acceptance identifier to a test source reference, and it never opened a test file. Both rules were satisfied by a repository where no test carried any identifier. The validator now reads `paths.testRoots`, collects all three forms where each tool puts them, reports a citation naming no declared criterion, and reports a page claiming `verified` while a criterion of its own goes uncited. The run prints how many declared criteria any test cites.
- Added `paths.testRoots` to `schemas/standards-project.schema.json`. The scan reads only what a consumer declares, so a repository with no test root states that position rather than passing unread.
- Extended `tools/validate-parity.mjs` with the mapping pass, because parity already owns the relation between a specification and the code it names and a sibling tool would own one concept twice. The pass builds its indexes from one text scan, so the tool stays local and deterministic with no build. The new `parity.sourceRoots` field names the directories a mapping resolves in, defaulting to the directory holding the solution.
- Registered the `CONSUMERS` topic and returned `provisionRegistry.topics` to alphabetical order. `METHODS` had been filed before `MESSAGES` since the release that added it.
- One sentence in the workflow concept of `docs/core/system.md` read `the system remembers progress`. It now reads `the system stores progress`, which is what a durable store does.

## v1.18.0

- Added `BACKEND.API.ROUTES.002`, which states a state transition the five HTTP verbs cannot carry as a POST custom method whose path ends with a colon-suffixed verb, per AIP-136. `BACKEND.API.ROUTES.001` left the transition form undecided, so one consumer mixed state-noun subresources (`/closure`, `/cancellation`, `/retirement`) with bare verb segments (`/abandon`, `/complete`) across the same surface, and a caller could derive neither shape from the other.
- Added `BACKEND.API.METHODS.001` and `BACKEND.API.METHODS.002`. The first applies PATCH with `application/merge-patch+json` to a partial update under RFC 5789 and RFC 7386; the second reserves PUT for replacing the whole resource its path names. A PUT-only surface forced every partial change to carry either a full body or an unreviewed per-field merge rule.
- Added `BACKEND.API.CONVENTION.006`, which carries a monetary value as one `Money` transport object holding `amount` and an ISO 4217 `currency`. `BACKEND.DOMAIN.MONEY.001` held the shape in the Domain while the transport still split it into sibling fields paired only by a server invariant the schema could not see.
- Added `BACKEND.API.CONVENTION.007`, one field-naming rule set for transport models: no `is`, `can`, or `has` boolean prefix, one `At`/`From`/`Until` suffix per time shape, `languageTag` per RFC 5646, `ianaTimeZone`, `currency` without `Code`, `code` for a categorised outcome beside `reason` for its prose, one path-parameter name per resource, and a ban on the noise words `details`, `info`, `metadata`, `value`, and `keyName`.
- Amended `BACKEND.API.ERROR.001` to name the `code` and `detail` pair on a problem body and to cite `BACKEND.API.CONVENTION.007` for the `code` and `reason` pair on a request body.
- Added `EXT.LOCALE.MESSAGES.004`, which writes an empty-field validation message as an imperative naming the field and a format or length message as a descriptive statement with the concrete bound, after the GOV.UK Design System error-message pattern. Added `EXT.LOCALE.MESSAGES.005`, which requires a screen to resolve every machine identifier it renders to a humanized catalog label.
- Added the `mcp` extension, `docs/ext/mcp.md`, with `EXT.MCP.SERVER.001` and `EXT.MCP.TOOLS.001` and `EXT.MCP.CONVENTION.001`. `SERVER.001` requires the initialize `instructions` value to state transport, audience, write surface, and refusal behavior, because the MCP client loads server instructions once and tool descriptions per call. `TOOLS.001` requires an imperative title, a declarative description, positive parameter polarity, and stated side effects.
- Registered the `METHODS` topic in the provision registry, and bumped the consumer pin to this release.

## v1.17.0

- Added seven provisions to `docs/frontend/ui.md`, which turn the controlled UI contract from a set of declarations into a pipeline a validator walks. `FRONTEND.UI.DESIGN.001` requires a root `DESIGN.md` carrying the brand, tokens, vocabulary, patterns, do list, refusals, motion, and voice, because an agent that has read no contract picks its own and the picks differ on the next page. `FRONTEND.UI.COMPOSITION.002` and `FRONTEND.UI.COMPOSITION.001` make a repeated page shape a catalog recipe with a schema-valid sidecar, and make a page region name one rather than invent a layout. `FRONTEND.UI.STATE.001` refuses a sidecar that declares a state no component any region names carries, which is the shape of a page that lists `saved` and announces nothing. `FRONTEND.UI.GATES.001` refuses an implementation that renders a `data-region` the sidecar does not name, which is the section an agent added after freezing its plan. `FRONTEND.UI.PLACEMENT.001` and `FRONTEND.UI.ACCEPTANCE.001` put the acceptance record and its files in an `evidence` directory beside the route, and refuse an identifier with no record or no file.
- Read the shell when resolving a declared state. A shell renders around every page, so a state one of its own components carries is a state every page under it has. A door client whose layout renders the connection indicator was reporting `offline` as a state no page region answered for, which was true of the region and false of the page.
- Rewrote `FRONTEND.UI.PROTOCOL.001` and tightened `FRONTEND.UI.PAGE.001`. The protocol asked an agent to search, compose, and record, and every step of it was checked by the agent that performed it. It now names four gates whose output is artifacts: a sidecar a validator reads and acceptance files a browser runs. The page rule now covers every route in the application tree rather than a page somebody judged non-trivial, and `schemas/ui-page.schema.json` gains a required `layout` from a closed set of five shapes, a `hiddenWhen` on a region, and the `row-expanded` state.
- Extended `FRONTEND.UI.EVIDENCE.001` with the matrix its own requirement had been naming. The provision said an author names evidence from the matrix in that section, and the section held no matrix.
- Added `schemas/design-contract.schema.json`, `schemas/composition-recipe.schema.json`, and `schemas/acceptance-criteria.schema.json`, with the `design-contract.md`, `composition.md`, `composition-recipe.json`, and `acceptance-criteria.json` templates a consumer copies. `standards.project.json` gains `paths.uiCompositions` for the catalog root.
- Moved the local JSON Schema evaluator into `tools/schema.mjs`. `tools/validate-ui.mjs` needed it for three new schemas, and a second copy would have drifted from the first in the way `tools/prose.mjs` already exists to prevent. The keyword gate moves with it, so a schema carrying a keyword no branch implements still fails rather than passing unread.
- Replaced `BACKEND.ARCHITECTURE.WORKER.001` with `BACKEND.ARCHITECTURE.WORKER.002` and `BACKEND.ARCHITECTURE.WORKER.003`. The old rule restricted when a Worker project may exist and never said where durable dispatch, queue consumption, workflow advancement, and scheduled work run without one. A consumer could therefore host four background services inside WebApi, deviate from no rule it could name, and record no decision, which is what one consumer did. The first new provision requires the execution host to be declared whichever it is, and the second states the three conditions that make a separate process the answer: independent scaling, independent deployment, and isolation from request-path resource limits. The outbox, jobs, and report extensions now activate the declaration rather than the project.
- Rewrote the boundary half of `BACKEND.DOMAIN.CLOSEDSET.001`. The rule forbade enums in Domain and the prose after its example then described a transport enum, a persistence discriminator, and an Application mirror in three paragraphs that read as permission. A reader following the rule refused enums everywhere and a reader following the rationale accepted them at the boundary. The four layers now sit in one table with one permitted form each, the transport enum has one stated condition, and the conversion back to a record case before a Domain call is written down, because without it the boundary exception readmits the integer the rule removed.
- Added `BACKEND.DOMAIN.MONEY.002`, which refuses `double` and `float` for a monetary amount, a rate applied to one, and a quantity compared for exact equality. The prohibition was a sentence in the rationale of `BACKEND.DOMAIN.MONEY.001`, where it obliged nobody and no verification row reached it.
- Reconciled `BACKEND.DOMAIN.ID.001` with `BACKEND.DOMAIN.TIME.001`. Version 7 embeds a timestamp, so the identity factory reads a clock that the no-clock rule appeared to forbid. The carve-out is now stated with its reason, and the collision question RFC 9562 leaves open is answered: the Domain writes no handling for it and a duplicate key fails like any other write conflict.
- Permitted the nested state-case form in `BACKEND.DOMAIN.CONVENTION.003` and `WORKSPACE.NAMING.SUFFIX.001`, and restated `WORKSPACE.NAMING.AGGREGATE.001` against the qualified name. `OrderState.AwaitingPayment` already leads with its aggregate through its declaring type, and the flat rule read as requiring `OrderAwaitingPaymentState`, which repeats the anchor twice. Both forms are now named, and a project picks one.
- Replaced `BACKEND.APPLICATION.MEDIATOR.001` with `BACKEND.APPLICATION.MEDIATOR.002` and moved the package to `BACKEND.APPLICATION.CONVENTION.005`. The Standard named two LiteBus members, so a consumer changing mediator overrode a Standard to keep a property the Standard was not actually about. The obligation is that the write and read paths stay separately dispatched; the members that supply it are a replaceable default.
- Titled `docs/backend/persistence.md` "Persistence" and added a Concepts section dividing its provisions into provider-neutral and provider-specific groups. The page was titled "Marten Persistence" while the manifest pinned EF Core for its own extension, so a consumer on the extension could not tell which of the twelve provisions moved with the provider. `COMMIT.001`, `NAMING.001`, and `SCHEMA.001` now name what each provider does, and `SCHEMA.001` says what a reviewed step is instead of leaving the tool unnamed.
- Added `WORKSPACE.STRUCTURE.DOTNET.002`, which permits one workspace-root solution listing more than one deployable unit. A workspace with an API and a command-line tool had no single file an editor could open across both, and the existing rule read as forbidding one. The API solution stays at `apps/api/` and stays the file `paths.apiSolution` names. The same change marks `AppHost` and `ServiceDefaults` as conditional in the tree example, because they are local hosting projects rather than application layers and their absence needed no override.
- Added `QUALITY.SECURITY.AUTHZ.002`, which requires a protected collection query to apply its authorization scope as a database predicate before paging. `QUALITY.SECURITY.AUTHZ.001` states a check against a target, and a collection has no single target, so the sentence covering it lived in a rationale. Filtering a materialized result is the shape OWASP names as broken object level authorization, and a page assembled from an unrestricted read leaks the total count even when every row is dropped.
- Added `QUALITY.SECURITY.ABUSE.002`, which requires one shared limit store across replicas. A per-process counter multiplies the declared limit by the replica count, so the limit a specification states is not the limit the system applies, and the multiplier changes when the deployment scales. This was also a rationale sentence.
- Added `QUALITY.OPERATIONS.TRACE.001`, which names W3C Trace Context `traceparent` and one configured propagator. `QUALITY.OPERATIONS.OBSERVABILITY.001` required a trace identifier and left its format open, so two hosts could each satisfy it and correlate nothing across the boundary between them. `BACKEND.API.ERROR.001` now cites RFC 9457 rather than the obsoleted RFC 7807, and states that its `traceId` is that header's trace-id.
- Added `QUALITY.OPERATIONS.ALERTS.002`, `QUALITY.CI.RELEASE.002`, and `QUALITY.CI.PROTECTION.002`. A declared severity with no destination is a label. An artifact with no component inventory cannot be checked against an advisory without rebuilding the dependency graph of a commit somebody has to find first. A branch-protection bypass that nobody recorded is indistinguishable from a habit, and it is the only case where a commit on the default branch was not reviewed.
- Enumerated the nine WCAG 2.2 criteria under `FRONTEND.COMPONENTS.ACCESSIBILITY.001`, with the level each one carries. The rule listed five properties a component can satisfy alone, and the nine additions are properties of a flow or a page, so a project targeting AA had no statement of what it had taken on.
- Added `FRONTEND.DATA.OPTIMISTIC.002`, which requires a reconciled optimistic outcome to be announced through a live region. A rollback is a silent visual change, and a reader using a screen reader heard the optimistic value announced and nothing afterwards.
- Added `FRONTEND.DATA.CONVENTION.005`, which runs the pinned generator without `--immutable`. The pinned `openapi-fetch` release wraps an immutable array in a type carrying no array methods, so `.map` over a response collection stops compiling; the defect is open upstream and the generator flag is what reaches it.
- Stated in `FRONTEND.RENDERING.STATE.001` and `FRONTEND.COMPONENTS.STATE.001` why their state lists differ. One listed six and the other seven, which read as a contradiction rather than as a route having `not-found` and a control having `disabled` and `pending`. `schemas/ui-page.schema.json` is named as the complete set.
- Extended nine extensions with the value each rule left to the reader. `EXT.AUDIT.PROTECTION.002` names SHA-256 and canonical serialization, because two implementations that each chose their own cannot verify each other. `EXT.AUDIT.PROTECTION.005` asks for an external checkpoint, because a chain proves records are consistent with each other and not that the whole chain was never recomputed. `EXT.AUDIT.READ.002` audits a bulk read inside one boundary, which is the shape a copied dataset has. `EXT.CACHE.KEY.002` refuses personal data in a key, because a key space is a store nobody classified. `EXT.CACHE.INVALIDATE.003` records a manual clear and `EXT.CACHE.FAILURE.003` separates a miss from an error from a degraded response, because a fallback that works is invisible. `EXT.OUTBOX.STATE.001` requires sealed records rather than four labels, since three of the four states carry data the others do not. `EXT.CONCURRENCY.IDEMPOTENTKEY.005` adds the actor to the fingerprint, because two actors can present the same client key. `EXT.CONCURRENCY.IDEMPOTENTOUT.003` adds the caching headers, which are computed for the exchange rather than for the result. `EXT.TENANCY.STORAGE.001` resolves the tenant at session creation rather than per query. `EXT.TENANCY.OPERATION.002` and `EXT.LIFECYCLE.PURGE.004` record an all-tenant run and a legal-hold release, which are the two events no single tenant's own trail holds. `EXT.REPORT.CONTENT.003` refuses a formula-leading value on import, because escaping on the way out protects this export and not the next one. `EXT.JOBS.TIME.001` became a MUST with a named time zone, because a local schedule runs twice on one date each year. `EXT.LOCALE.FORMAT.001` names the `Intl` constructors, and `EXT.LOCALE.ROUTES.001` states the path segment as the baseline shape.
- Added `docs/reference/decision-records.md`, which states what a consumer decision record holds and which fields cite it. `overrides[].decision` pointed at a path the standards never described, and the two outside conventions this template extends were named nowhere. It also states the rule an override has to satisfy: an override whose decision body justifies less than the provision it names is the same defect as an unnamed decision.
- Added an amendment procedure to `CONTRIBUTING.md`. An active provision ID identifies its current assertion, and nothing said what to do with the retired number, the citations, the summary bullet, the verification row, or the fixture case.
- Annotated every `schemaVersion` pin with why it is a constant. A schema evolution model that accepts a higher minor is a compatibility guarantee between releases, which `CORE.AUTHORING.SNAPSHOT.004` refuses. Each release pins one exact shape and a consumer absorbs every difference when it adopts, so the constant is the model rather than an omission. The annotation is there so the question is answered where it is asked.
- Closed the gaps in the local JSON Schema evaluator rather than adding a library. `tools/` ships dependency-free Node with no install step, and the keyword gate is what makes a local evaluator safe: a keyword with no implementation fails the schema instead of passing unread. The gate now reaches every position a subschema can occupy, where it previously stopped at four, and the evaluator implements the upper bounds, `anyOf`, `prefixItems`, `contains`, `propertyNames`, `patternProperties`, and `dependentRequired` that a schema author had no way to express. A `oneOf` failure now carries the first reason from each branch that rejected, instead of a count.
- Replaced `fs.globSync` in `tools/validate-consumer.mjs` with a walker built on `fs.readdirSync`. The call arrived in Node 22 and is still experimental, so it put a runtime floor on every consumer without the repository stating one. The walker also skips build output, which is why the reported surface count fell: a generated `AssemblyInfo.cs` carried no vocabulary and its presence depended on whether somebody had built the project.
- Fixed three parsing defects in `tools/validate-ui.mjs`. A class string ending at an escaped quote shifted every later token in the file into the wrong parse. A CSS brace or comma inside a quoted attribute value was counted as structure, so `[data-token=",.promo"]` reported a feature style that was not there. The Tailwind variant list now composes, so `group-data-[state=open]` is a variant rather than an arbitrary selector. The source lock's fingerprint is also checked against the preset fields recorded beside it, not only against the consumer configuration.
- Added a page registry pass to `tools/validate-ui.mjs`. A page naming a frontend nobody declared was selected by no per-frontend pass and reported nothing, and two pages sharing a route were each valid alone.
- Tightened the parity depth test from three path segments to four. A handler sitting directly in an aggregate folder passed and derived its identifier from the aggregate name, which no specification can carry.
- Gave every command `--help` and every validator `--format=json`. A caller reading the result had to parse the human lines back apart, which breaks the first time a message is reworded. `tools/validate-consumer.mjs` also lifts each controlled-UI problem into its own list, because a structured caller has no output "above" to look at.
- Added exit-code assertions to the consumer case suite and resolved the thirteen templates whose placeholders the fixture never filled. The suite had been validating those templates against literal `__TERM__` strings, which satisfy every rule that asks for a non-empty value.
- Added a Node matrix, a nightly run, step timeouts, the parity case suite, and a pull-request body check to the workflow. The parity suite had been in no gate, and the "Skipped Checks" section the template asks for was never read.
- Exempted a rejection table's reason column from the vocabulary scan and the cell measure, and added the mannered column to the same exemption. A column that lists refused words was already exempt, and the column beside it that explains the refusal was not, so a glossary generated from its own language record reported its own reasons as breaches. The exemption applies only inside a table that already carries a rejection column, so an ordinary reason column elsewhere stays measured.
- Moved the manifest pins to the versions the release runs. Central package management enforces what the manifest pins, so drift between the two makes the rule point at a version nobody resolves. `WORKSPACE.DEPENDENCIES.PINS.001` now says what it always meant: the manifest pins the baseline stack, and a consumer's own product packages are pinned in the consumer's central file under a decision record, because the manifest belongs to the release rather than to the consumer.

## v1.16.0

- Added the `tutorial`, `how-to`, `reference`, and `command` page classes, and split the guide class into the first two of them. One class was carrying four jobs. Its contract, `Purpose, optional Prerequisites, Procedure, Verification`, was asked to hold a first lesson, a recipe for a known task, a list of settings, and a command reference, and each of those is read by a different person for a different reason. A tutorial ends in `What you built` rather than `Verification`, because verification asks whether a procedure worked and a tutorial asks what the reader now has. The how-to contract is the former guide contract under its own name, so `docs/guide/` needed no page moved.
- Mapped `docs/reference/` and `docs/tools/` to a page class, and widened the stem pattern that decides a class to accept a hyphen. The pattern had required a single lowercase word, which every descriptive file name fails, so `pageClass()` returned null for all three guide pages and for `audit-obligations.md`. The guide contract had therefore been enforced on no page in the tree since the class existed, and the four pages it was written for were the four it never read. Turning the reference directory on failed `audit-obligations.md` at once, and that page is now written to `Intent, Reference, optional Notes`.
- Added `CORE.AUTHORING.DISCLOSURE.001`, `CORE.AUTHORING.DISCLOSURE.002`, and `CORE.AUTHORING.DISCLOSURE.003`, which place a page at one layer and require every command page to name what it runs. A procedure that lists what a command already does becomes a second copy of that command, and the copy is the part that drifts. `Underneath` is a required section rather than an optional one, so a command page that hides its mechanism fails rather than passing in silence, and a command with nothing separately runnable writes `None.` under the existing empty-section rule. That makes the third provision static at no cost, where an optional section would have left it to review.
- Added `CORE.AUTHORING.CONFIG.001` and the `configuration` kind behind it. A setting introduced inside whichever procedure needed it first is findable only by the reader who already knows which procedure that was. A configuration page carries the default, the scope, and what overrides each setting, and states its precedence order once rather than per setting.
- Added `CORE.AUTHORING.INDEX.002` and `INDEX_MISSING_ROUTING`, which require the documentation root to name each page class before its first navigation group. A reader who cannot tell a tutorial from a reference opens both and trusts neither. The check confirms the block is present and positioned; whether a reader can act on it stays a review question, because no validator reads for comprehension.
- Added the `tutorial`, `how-to`, `reference`, `command`, and `configuration` consumer kinds, in `schemas/specification-metadata.schema.json` and in the `KINDS` map that holds the same rules a second time by design. A repository that ships a command had nowhere inside its documentation root to document it, so the command reference lived beside the code, outside the tree the validator reads, unchecked for vocabulary, prose, and link resolution. Each kind declares the H2 order its class answers at, which is the first time the consumer validator checks a section rather than a metadata field. A consumer tutorial had no kind that fit, and the nearest one, `section-index`, states that a page is a directory index. That is the misclassification `CORE.AUTHORING.METADATA.004` exists to refuse.
- Added `templates/standard/tutorial.md`, `templates/standard/command.md`, and the consumer templates for the three new kinds, and renamed `templates/standard/guide.md` to `how-to.md`. Added `docs/tools/`, which documents the five commands this repository ships as command pages. `tools/README.md` now links to them rather than restating them, which is the rule this release asks a consumer to follow.
- Added `CORE.SYSTEM.SCENARIO.001`, `CORE.SYSTEM.SCENARIO.002`, `CORE.SYSTEM.SCENARIO.003`, and the `scenario-cast` specification kind, which together require every module, aggregate, use-case, domain-policy, and end-to-end-flow specification to state one concrete occasion for its subject. Every section a specification already carried states a rule, a state, or a mapping, and none of them says when the behavior happens or who is under pressure while it does. In one consumer of 377 behavior specifications, not one page said what was going on in the world when its operation ran: a shortfall resolution was triggered by "somebody works through the open shortfalls", which names the mechanism and leaves the reader unable to tell that a buyer had paid for a ticket that did not exist. A reader who cannot place an operation reads each of its rules as an arbitrary constraint. The cast is one record rather than a habit, because independent scenarios invent an organization and a buyer per page and nothing then carries between two pages read in sequence.
- Bounded a `Scenario` section to 120 words in `CORE.SYSTEM.CONVENTION.007`, replaceable through `scenarioWordLimit` in `standards.project.json`, and refused a rule, acceptance, or end-to-end identifier inside one. Both bounds exist to keep the section from becoming a second specification: prose that grows past a paragraph becomes the page a reader reads instead of the tables, and an identifier inside a scenario reads as a second definition of the rule it names. The illustration has no authority, so a scenario that contradicts the page it sits on is the part that is wrong.
- Added a `Scenario` section to the module, aggregate, use-case, domain-policy, and end-to-end-flow templates, a `scenario-cast.md` template for the reference record, and a `Write the occasion before the rules` step to the domain modeling guide. Each template states what its own kind's scenario shows, because the occasion behind an aggregate is one instance moving through its states and the occasion behind a use case is one moment.
- Gave the consumer validator's case suite a third expectation form, `{ absent }`, for a diagnostic that must not appear. A case that narrows one setting can make an unrelated rule fire, and `no problems at all` then asserts the wrong thing: narrowing the documentation root put the reference cast outside the scan, which failed a case about an unscanned page.
- Split the contracts every layer names out of Application into a fifth baseline project, `Application.Abstractions`, and amended `BACKEND.ARCHITECTURE.PROJECTS.001` to require it. Four projects left the cross-cutting contracts with nowhere to go but a folder inside Application, and a folder cannot decline what a developer puts in it. In one consumer that folder had reached 181 files and a tenth of the layer, with 23 children of which 8 carried the name of a sibling module, 99 files belonging to exactly one aggregate or module, and one file importing a use-case namespace. The project references Domain alone, so the compiler now refuses what review had been asked to catch: nothing in it can name a use case, a handler, or a module.
- Added `BACKEND.ARCHITECTURE.PLACEMENT.001`, which places a type at the lowest folder holding every consumer that names it. Placement had been a judgement call resolved by the word "shared", which is a claim about the future rather than a fact about the code, so it was never wrong at the moment it was made and always wrong later. Counting consumers is mechanical, and it derives the contracts project as the residue of the count rather than as a destination. Domain's shared kernel stays, because `BACKEND.ARCHITECTURE.CONTRACTS.001` closes its membership.
- Added `BACKEND.APPLICATION.PORT.002`, which declares a port where its consumers meet rather than in a project-wide folder of ports. `BACKEND.APPLICATION.STRUCTURE.001` already rejects grouping by technical type one level down, and a folder of every port in the system is the same mistake with a different noun. A port beside its aggregate is reviewed with that aggregate's specification; a port in a central folder is reviewed by nobody, which is how a consumer accumulated 54 single-module ports outside the module tree.
- Stated in `BACKEND.APPLICATION.CONVENTION.001` that only a use case gets a folder, and that anything else sits flat in the folder that owns it. Four different shapes for module-local code had grown in one consumer, including a `Shared` folder inside a module, and a folder named for a service reads exactly like a folder named for a use case until you open it.
- Replaced the `Shared` children in the Application layout example with the placement rule, and added `Pipeline/` for the pipeline's own stages. The example had said "only for types used by multiple modules" in a closing sentence and shown a folder of technical kinds above it, and the picture is what gets copied.
- Made the consumer validator fail closed on an unclassified file, and added `CORE.AUTHORING.METADATA.004` to state the rule. A Markdown file with no metadata block was skipped in silence and the run still reported PASS, so an unvalidated specification was indistinguishable from a sound one. A real consumer's domain policy index sat unchecked indefinitely while every page around it was validated. The file already carried the principle in a comment, and applied it only to a metadata object in the wrong wrapper. The error now names the file and the two ways out, because a directory index that owns no aggregate, use case, or policy declares `section-index` rather than an exemption. `paths.unstructuredDocs` declares the paths whose Markdown is navigation or prose, and each entry must be a repository-relative path that exists, so an exemption cannot be a typo.
- Resolved the documentation root from `paths.docs` and failed a scan that walks no Markdown. The root was hard-coded to `docs` while `paths.domainDocs` was configurable, so a consumer whose specifications live anywhere else scanned zero files and passed. That is the same fail-open shape as an unclassified file, one level up, and shipping the leaf without the root would have left the larger hole open. The report names the root it walked, so a thin scan is visible on a passing run rather than only on a failing one. The root is an explicit field rather than the common ancestor of `domainDocs` and `uiDocs`, because that ancestor is the repository root whenever the two live in separate trees, which widens the scan instead of narrowing it.
- Required every configured path to resolve. `domainDocs`, `uiDocs`, `apiSolution`, and each frontend path were accepted unread, so a typo disabled the use-case, workflow, and policy cross-file checks without a word. Each message now names the field, the value, and the checks the missing path silently switched off.
- Named the allowed values in every closed-set error, and described the expected form in every pattern error. `bad risk 'consistency'` stated what was wrong and never what was right, and the vocabulary existed only inside the validator, so three authors working from the templates independently invented `abuse`, `consistency`, and `privacy`. Each message now carries `expected one of ...`, matching the shape `platform` already used, and the kind list derives from the kind table rather than repeating it. A pattern error describes the form in words with an example, rather than printing a regular expression or the bare phrase `fails pattern`.
- Added `prohibitedKinds` so a consumer can declare a specification kind it does not use. A project whose agent instructions prohibited workflow specifications outright had no way to enforce that: the kind stayed valid, the template stayed on disk, and an agent reaching for it received a PASS while breaking the project's most explicit rule. The declaration makes the prohibition a contract rather than advice, and each entry must name a kind the validator knows, so a typo prohibits nothing visibly rather than silently.
- Added `CORE.SYSTEM.COVERAGE.001` and `tools/validate-parity.mjs`, which compares Application operation folders with use-case specifications in both directions. Nothing had ever compared the documentation with the code, and both directions hid real work: in one consumer fifteen aggregate roots carried a repository, persistence registration, and dependency wiring while no use case reached any of them, and eighty-one operations had no specification. Code with no specification reads as a reviewed feature and is not one. A `planned` specification with no handler is correct and is not reported. `--report` prints the same findings and exits zero, for a consumer burning down a backlog, and the failing behavior is the default. `parity` in the project file names the Application project when convention cannot resolve it, and carries the two ignore lists.
- Added `CORE.AUTHORING.DERIVED.001`, which requires a specification to name the source or the command behind a count, a census, or a membership list rather than restating it. Three hand-maintained registries in one consumer had drifted from the code they described, and one of them said so in its own text while staying wrong. A restated fact is accurate until the next unrelated commit, nothing reports the moment it stops being accurate, and a reader trusts a stale number exactly as much as a fresh one.
- Added `CORE.AUTHORING.PROSE.002` and `CORE.AUTHORING.PROSE.003`, which apply the controlled prose measures to a consumer specification tree and give the accepted debt an exit. The profile had existed since the repository did, and `tools/validate-standards.mjs` enforced it over these pages alone. A consumer inherited the rule and no check: one project of 459 pages carried 5694 measure violations while its validator reported a pass, because the run never looked at prose. A baseline records the count and the review date a page was accepted at, and `CORE.AUTHORING.PROSE.003` retires the entry when `lastReviewed` advances, so the debt burns down against the rereading a project already performs rather than through a separate campaign nobody schedules.
- Added `CORE.AUTHORING.TERM.002` and `CORE.AUTHORING.TERM.003`, and `schemas/language.schema.json` behind them. Terminology governance had been prose: a glossary column headed `Avoid` and a module table headed `Rejected synonyms`. Prose cannot be checked against prose, so both stated the rule to a reader and to nothing else. In one consumer the glossary defined `Seller` as the organizer and one module used the same word 21 times for the buyer facing that organizer, with both statements approved and neither reported. A rejection carries the scope it binds, because a word correct in one module is wrong in another, and the scope is a path expression rather than a module name so a policy page and a research note fall under it too.
- Added `CORE.AUTHORING.VOICE.002`, which refuses metaphor where a literal phrase carries the same meaning. A figure of speech defeats terminology review rather than merely reading poorly: a reader cannot tell whether it names a defined concept or decorates one, so it is never entered in a glossary and never rejected as a synonym. It also asserts more than its author chose. Three settlement routes described as rungs on a ladder claim an ordering the routes do not have, and the consumer that wrote it had no fourth route to place above or below them.
- Extracted the prose measures into `tools/prose.mjs`, which both validators now read. The measures had one implementation and one caller, so extending them to consumers would have produced a second copy, and a second copy of a rule is the drift the rule exists to prevent: each validator would keep reporting a pass against its own idea of the profile. `tools/validate-standards.mjs` produces byte-identical output across the change.
- Added `paths.language` and `prose.baseline` to `schemas/standards-project.schema.json`, and `templates/consumer/language.json` with its row in the template index. A language record that does not resolve names the checks its absence switches off, and a scope that is not a regular expression is an error rather than a rejection that silently matches nothing.
- Added `CORE.AUTHORING.TERM.004` and `paths.languageScan`, which take the vocabulary check to the surfaces a Markdown scan never reads. `CORE.AUTHORING.TERM.003` covers a consumer's specification tree alone, which is usually the smallest surface its words reach and the only one nobody drifts on. The consumer that motivated the rule has 459 pages, 4,390 source files, 3,458 interface strings, 1,005 API property names and 34 feature files, and every collision its own audit found lived outside the pages: one word naming five things, another naming the organizer in the glossary and the consumer in the shipped dashboard, in one dictionary file, in both languages. A pattern matching no file is an error rather than a quiet skip, because a surface switched off in silence reports the same pass as a surface that is clean.
- Added `checkLanguageInSource` to `tools/prose.mjs`, which scans a non-Markdown surface for the same two problems. It differs from the Markdown scan in three ways, and each one is a rule about what a word looks like outside prose. The fenced-block and rejection-column exemptions do not apply, because a source file is code throughout and has no column that exists to list refused words. Each line is read twice, once as written and once with identifier boundaries opened out, so `SellerOrderId` reports a rejection of `seller` while `load-bearing` survives the pass that would have split it; a hit found by both passes is reported once. A scope is matched against the repository-relative path for these files, because a file outside the documentation tree shares no other root with a page.

## v1.15.0

- Added the `audit` extension at `docs/ext/audit.md`. It owns the producer, the transaction boundary, the selection policy, the store, and the read path for an audit trail, and it replaces `QUALITY.SECURITY.AUDIT.001`. The baseline provision states seven required record fields and stops there, so a consumer that satisfied it could still record successes alone, write from inside a handler that never runs on refusal, keep no request origin, and hold the trail in a table its own account could rewrite. The extension states the producer boundary that makes an outcome observable, requires a record for a refused authorization, separates the atomic success write from the unsuccessful write that its own rolled-back transaction cannot carry, and enforces append-only through storage privilege rather than through the absence of a code path. Selection is total: every Command declares audited or excluded with a reason, and an automated check fails the build on a Command that declares neither.
- Added `docs/reference/audit-obligations.md`, which maps each `EXT.AUDIT` provision to the external obligation it satisfies. It covers the NIST SP 800-53 AU family, ISO/IEC 27001:2022 Annex A 8.15, PCI DSS 4.0 Requirement 10, the OWASP logging guidance, and the GDPR articles that pull against each other. `EXT.AUDIT.ADOPT.001` asks a project to justify its selection, and the justification was previously a survey each project repeated.
- Added `docs/guide/audit-trail.md`, which states one compliant implementation on the LiteBus command pipeline. It carries the concrete record shape, the pre-handler, post-handler, and error-handler stages, the handler priority that orders the audit write before the business commit, and the second connection that carries a record its own rolled-back transaction cannot.
- Required `platform` on every `paths.frontends` entry, in the schema and in `tools/validate-consumer.mjs`. The field had been optional on its own, so a Next.js frontend with shadcn/ui installed could omit it, skip every `FRONTEND.UI` provision, and receive a PASS. A frontend outside the controlled UI contract now declares `other-web` or records an override, which is a visible statement rather than an omission. `tools/validate-ui.mjs` also names the frontends it skipped instead of reporting only a count.
- Checked `selectedExtensions` against the manifest in `tools/validate-consumer.mjs`. It compared `applicableExtensions` to `selectedExtensions` and compared neither to `standards.manifest.json`, so two consistent lists of ids that no longer exist validated cleanly. A release that renames an extension id now produces an error naming the known ids instead of silence.
- Extended `selectedExtensions` to accept `{ id, criterion, reviewBy }` beside a bare id. `CORE.SCOPE.EXTENSIONS.001` requires selecting an extension only when its criteria apply, and nothing recorded which criterion was met. `tools/validate-consumer.mjs` reports a selection whose `reviewBy` has passed, so a selection that costs nothing to keep expires instead of accumulating.
- Widened `applicableKinds` for four extensions to the kinds where each concern is specified rather than implemented. `concurrency` gains `module` and `aggregate`, because the aggregate specification owns the state mapping and the transitions. `integrations` gains `domain-policy` and `end-to-end-flow`. `outbox` and `jobs` gain `end-to-end-flow`, which is where a reader learns that a step is idempotent, that another runs off durable delivery, and that a hold expires on a timer. `docs/ext/README.md` states the rule that produced the sets.
- Removed the flat allowance from `CORE.SYSTEM.CONVENTION.002`. A module README carries `kind: module` and one file cannot carry two kinds, so a flat single-aggregate module had nowhere to put the aggregate specification that `CORE.SYSTEM.STATE.001` requires. Every module now groups its use cases under an aggregate subdirectory, which costs one repeated path segment where the names coincide.
- Added the `section-index` kind for a directory index that claims no implemented behavior and owns no aggregate, use case, or policy. A frontend architecture index and an evidence register had no kind that fit, so one was carrying `domain-index`, which the validator accepted. Unlike the other index kinds it may repeat, and `templates/consumer/section-index.md` is its template. `tools/validate-consumer.mjs` now enforces at most one `domain-index`, `glossary`, and `modules-index`, where only `product` had been counted.
- Added `CORE.SYSTEM.USECASE.002`, which defines `implemented`. The schema accepted `planned`, `implemented`, and `verified`, and `CORE.SYSTEM.USECASE.001` defined only the two ends. Working code whose acceptance criteria are not yet proven is the ordinary middle state, and leaving it undefined made the value a matter of taste.
- Added `TEMPLATE_INDEX_PATH` to `tools/validate-standards.mjs`. It resolves every template named in `templates/consumer/README.md` and reports a tracked template that no row names. The index is the only map from a template to the consumer path it targets, and a row pointing at nothing read exactly like a correct one. `templates/consumer/module-index.md` is `modules-index.md` again, which is the `kind` its own metadata declares.
- Implemented `oneOf` in the schema validator inside `tools/validate-standards.mjs`. The keyword had been rejected as unsupported, and adding it to the allowed list without the branch check would have skipped validation silently.
- Added issue forms for defects and proposals under `.github/ISSUE_TEMPLATE/`. The proposal form asks for the constraint before the change, and asks whether the change contradicts an active provision, because that provision has to be amended rather than worked around.

## v1.14.0

- Replaced the three-part provision identifier with `AREA.PAGE.TOPIC.NNN` and made the page path state it. `docs/frontend/components.md` owns `FRONTEND.COMPONENTS`, so `FRONTEND.COMPONENTS.OWNERSHIP.001` resolves to a file with no lookup. Nothing declares the mapping: the directory gives the area and the file stem gives the page. The old prefix labelled the subject area and served as the global page-uniqueness key at the same time, so 33 baseline prefixes competed in one flat namespace and peer pages lost their natural names.
- Flattened `docs/conventions/`. It encoded a foundation-versus-convention page class split whose two section contracts were identical, which made the distinction unverifiable. The two classes collapse into `topic`, and each of the eight areas is now one directory under `docs/`: `core`, `profile`, `workspace`, `backend`, `frontend`, `blazor`, `quality`, and `ext`.
- Renamed 26 pages so the file stem is its `PAGE` segment. `persistence-marten.md` is `persistence.md`, `ui-governance.md` is `ui.md`, `data-and-state.md` is `data.md`, `api-compatibility.md` is `compat.md`, and `dotnet-nextjs.md` is `nextjs.md`. Every H1 already carried the full title, so no information moved out of the repository.
- Renamed the six Blazor client prefixes. `CLIENT`, `CLIENTUI`, `CLIENTDATA`, `CLIENTRENDER`, `CLIENTTEST`, and `BROWSER` existed only because `FRONTEND`, `COMPONENT`, `DATA`, `RENDER`, and `FTEST` were already taken by React. They are `BLAZOR.STRUCTURE`, `BLAZOR.COMPONENTS`, `BLAZOR.DATA`, `BLAZOR.RENDERING`, `BLAZOR.TESTING`, and `BLAZOR.BROWSER`, and the pages are titled Blazor rather than Client.
- Renamed `PROFILE` to `PROFILE.NEXTJS` and `PROFILE.BLAZOR`. The generic word had belonged to one specific profile, and `AGENTS.md` cited `PROFILE.VERSIONS.001` for manifest version pins even though it is profile-neutral, pointing a Blazor consumer at the Next.js page.
- Renamed three extension namespaces that disagreed with their page names. `EXT.API` is `EXT.COMPAT`, `EXT.DATA` is `EXT.LIFECYCLE`, and `EXT.EXTERNAL` is `EXT.INTEGRATIONS`. This removes the `EXT.API` and `API` collision and the `EXT.DATA` and `DATA` collision, where one word named two concepts at two depths.
- Aligned the fifteen manifest extension keys with their file stems, so one word now serves as the manifest key, the file stem, the `EXT` namespace, and the `selectedExtensions` value.
- Renamed `WRITING.CONVENTION.001` to `CORE.AUTHORING.DEFAULTS.001`. It was a Standard carrying the reserved `CONVENTION` segment, which is the only signal of normative force inside an identifier. `CORE.AUTHORING.IDENTIFIER.002` now prohibits that shape.
- Closed the topic vocabulary in `provisionRegistry.topics`. Fourteen concepts had carried both a singular and a plural spelling, and six more carried two words: `AUTH` became `ACCESS` alongside `AUTHN` and `AUTHZ`, `DOCUMENT` named both a Marten document and a specification document, and `ENFORCE`, `OWNER`, `DOCUMENTATION`, and `MODELS` each collapsed into one form. Three provisions repeated their page name as their topic, so `WORKSPACE.STRUCTURE.STRUCTURE.001` and two siblings became `.TREE.001`.
- Added `docs/reference/provisions.md`, a generated index resolving every provision ID to its heading, its owning page, and a link to its anchor. There was no lookup table before, and `AGENTS.md` alone cites 46 distinct identifiers. `tools/generate-provisions.mjs` writes it, `tools/provisions.mjs` builds the content for both the generator and the validator, and `CORE.AUTHORING.INDEX.001` requires the checked-in page to match.
- Added `overridePolicy.requiresReviewBy` to the manifest. The review-date requirement on a UI rule override was a hardcoded `^UI\.` test in `tools/validate-ui.mjs`, `tools/validate-consumer.mjs`, and `schemas/standards-project.schema.json`, so a policy lived in a string prefix. It is now declared data.
- Consolidated the identifier terms. `Rule ID` and `Provision ID` were two glossary entries for one concept, while `rule identifier` separately named a consumer `INV-` or `POL-` identifier. The first sense is `Provision ID` everywhere, including the `overrides[].provisionId` field. The second is `Domain rule ID`, with its own entry. The glossary also gains `Page`, `Page class`, and `Page scope`, which were used normatively without a definition.
- Corrected the Reference example in the authoring standard. It illustrated the provision contract with `ARCH.DOMAIN.001` and the requirement text `Keep Domain package-free`, while the real provision is `Keep business invariants in Domain` and package references belong to `ARCH.DEPENDENCIES.001`. The validator passed because the identifier existed.
- Moved `quality/backend-testing.md` to `backend/testing.md`. Backend testing sat under quality while frontend testing sat under frontend.
- Renamed `templates/docs/` to `templates/consumer/`, which stopped it reading as a mirror of the repository's own `docs/`, and `templates/standards/` to `templates/standard/` with `normative-topic.md` as `topic.md` after the page class it templates.
- Realigned the manifest load plan keys to the area taxonomy. `repository.writing` is `core.authoring`, `testing.backend` is `backend.testing`, `client.*` is `blazor.*`, and `security.review` is `quality.security`. Added the `blazor.components` and `blazor.browser` plans, which the Blazor pages needed for an agent to select the narrowest task.
- Added `CORE.AUTHORING.IDENTIFIER.001`, `.002`, `.003`, and `CORE.AUTHORING.INDEX.001`. The authoring standard states the grammar, the reserved segment, the closed vocabulary, and the derived index as a `Provision identity` concept.
- Aligned the validator vocabulary with the standards. Fourteen `RULE_*` diagnostics fire only inside a Standards block and are `STANDARD_*`. Six `DOC_*` codes describe pages and are `PAGE_*`. `CONVENTION_ID` covered two opposite defects and splits into `CONVENTION_ID_SEGMENT` and `STANDARD_ID_SEGMENT`. `ID_PREFIX_OWNERSHIP` named a prefix concept that no longer exists and is `ID_SCOPE_MISMATCH`. The declared code list is generated from the codes the validator emits. `documentClass`, `parseDocument`, and `profiles[].documents` say page.
- Tightened the identifier grammar in `tools/validate-standards.mjs` and `schemas/standards-project.schema.json` to exactly four segments with a three-digit sequence. Every citation scanner shares one pattern requiring that tail, so a stale three-part identifier still reports as an unknown reference while grammar notation such as `AREA.PAGE.TOPIC.NNN` no longer produces a false positive.
- Moved `standards.manifest.json` to `schemaVersion` 4 for the `provisionRegistry`, `overridePolicy`, `paths`, and `profiles[].pages` fields.
- Grew the validator fixture suite from 84 to 95 cases, covering every new diagnostic with a failing fixture.

## v1.13.0

- Added the `dotnet-blazor` platform profile for a product that runs entirely in the browser. It composes the workspace, application core, quality, and operations conventions, replaces the Next.js frontend set with six client conventions, and names every excluded baseline with its reason.
- Added the client conventions under `docs/conventions/frontend-blazor/`: structure, rendering and routes, components, data and state, browser persistence, and testing. Each page owns one identifier prefix and states its obligations in the current provision form.
- Added four client load plans to `standards.manifest.json` so an agent can route a task on the Blazor profile.
- Added `SCOPE.BACKEND.001`. A consumer with no backend omits `paths.apiSolution` and records a decision naming every baseline rule left without a surface. Adding a backend to obtain conformance would contradict `CORE.COMPLEXITY.001`.
- Made `paths.apiSolution` optional in `schemas/standards-project.schema.json` for that case.
- Added `WRITING.ASCII.002`. An ASCII check excludes only the natural-language content paths that the project record declares, and every document about that content stays in scope.
- Made `tools/validate-ui.mjs` skip generated build output and native runtime asset directories. Scanning them reported the bundler's own CSS as a source violation that no consumer could fix. Authored feature CSS is still rejected.

## v1.12.0

- Repaired every baseline provision whose Requirement or Default only restated its own heading. The v1.11.0 conversion had moved each real obligation into `Rationale`, which the authoring standard declares informative, leaving the baseline formally non-normative. All 289 provisions across the 19 conventions, the platform profile, and the engineering system foundation now state their obligation in the normative block.
- Rewrote every templated evidence row. A verification row now names the command, path, test, or assertion that produces the result instead of repeating its provision heading.
- Rewrote every Agent Summary bullet that repeated its provision verbatim, so Tier 1 compresses Tier 2 again rather than duplicating it.
- Split compound provisions that carried two normative modals into separate identified assertions, including `API.ENDPOINTS.002`, `API.BOUNDARY.002`, `API.ACTOR.002`, `API.ERRORS.002`, `API.STATUS.002`, `API.OPENAPI.004`, `API.MODELS.002`, and `AGENTIC.EXTENSIONS.002`.
- Gave each page one Standards identifier prefix. `MARTEN.CONVENTION.*` became `PERSIST.CONVENTION.*`, component provisions became `COMPONENT.*`, rendering provisions became `RENDER.*`, `STATE.*` and `FORM.*` merged into `DATA.*`, and idempotency provisions moved under `EXT.CONCURRENCY.*`.
- Replaced the expiring ESLint note in the platform profile with `CONFIG.ESLINT.001`, which requires a concrete `settings.react.version` in the flat config.
- Reduced the extension index to a selection table and cited pointers. Its numbered activation procedure and uncited prohibitions duplicated `SCOPE.EXTENSIONS.001`, `CORE.COMPLEXITY.002`, and `AGENTIC.EXTENSIONS.001`.
- Linked every step of `docs/guides/getting-started.md` to the standard that governs it and grouped the steps into pin, configure, structure, specify, implement, and operate stages.
- Replaced the duplicated consumer gate commands in `AGENTS.md` with a citation to `CI.GATES.001`, which owns them.
- Added the `Standard` and `Workspace` glossary terms and renamed the `Repository` entry to `Repository (Domain port)`, so the Git sense and the Domain port sense no longer share one term.
- Renamed `docs/conventions/repository/` to `docs/conventions/workspace/` and retitled its structure page. The directory was named for the Git sense of `repository` while the glossary defined only the Domain port sense.
- Normalized Git-sense prose to `workspace`, so `repository` in a provision now means the Domain port unless it is qualified.
- Made `docs/reference/glossary.md` the single definition site. The engineering system Concepts section defined sixteen terms that the glossary also defined; it now carries only the relationships, distinctions, and worked examples that a one-sentence entry cannot hold.
- Added `WRITING.SNAPSHOT.006` and the `reviewedStandardsVersion` field in `standards.project.json`. `node standards/tools/validate-consumer.mjs` now fails when that value differs from the pinned manifest version, so adopting a release is an explicit act rather than a silent one. A provision can gain force while keeping its identifier, and 654 of the 709 identifiers on the previous release kept their string through this one.
- Promoted every authoring rule to an error. `WARNING_DIAGNOSTIC_CODES` is now empty, so a provision that restates its heading, an evidence row that names no artifact, a summary that repeats its provision, or a page with two identifier prefixes fails the build.

## v1.11.0

- Added `docs/foundations/authoring-standard.md` as the canonical contract for controlled technical prose, page structure, provisions, summaries, examples, and evidence mappings.
- Adopted an STE-inspired repository profile with 20-word procedure sentences, 25-word descriptive sentences, six-sentence paragraphs, ASCII prose, active voice, and controlled terminology. The profile does not claim ASD-STE100 conformance or reproduce its dictionary.
- Limited normative vocabulary to uppercase `MUST`, `MUST NOT`, `SHOULD`, `SHOULD NOT`, and `MAY` under the RFC 2119 and RFC 8174 interpretation.
- Rewrote every active foundation, profile, convention, and extension page with explicit Requirement, Default, Replacement, Agent Summary, and Verification structures.
- Added canonical IDs to actionable conventions while retaining distinct replacement authority from Standards overrides.
- Replaced duplicated policy prose in `AGENTS.md` and `CONTRIBUTING.md` with concise projections that cite canonical provisions.
- Added `tools/validate-standards.mjs`, its dependency-free fixture suite, a pull request checklist, and a pinned GitHub Actions validation workflow.
- Added authoring templates for normative topics, extensions, and guides under `templates/standards/`.
- Removed the repository writing convention after moving its authority into the authoring foundation.
- Made the standards release model normative. `WRITING.SNAPSHOT.003` requires each release to state its complete contract without depending on an earlier release. `WRITING.SNAPSHOT.004` prohibits a compatibility guarantee, migration path, deprecation period, replacement map, or identifier alias between standards releases. `WRITING.SNAPSHOT.005` records that a consumer keeps a pinned release for as long as that consumer chooses.
- Scoped that release model to the standards repository. Consumer product API compatibility, schema migration, deprecation, and rollback provisions are unchanged, including the `api-compatibility` and `persistence-ef-core` extensions.
- Moved the release model and version-number meaning from `CONTRIBUTING.md` into the authoring standard as the canonical `Release model` concept. `CONTRIBUTING.md` now projects that model with citations instead of restating it.
- Added the `Standards release` glossary term.
- Corrected `CORE.DOCUMENTS.003`, which named only `planned` and `verified` while the metadata schema, `AGENTIC.METADATA.001`, and `tools/validate-consumer.mjs` all accept `implemented`. The provision now requires an implementation status and cites the schema as the owner of the permitted values.
- Stated the Specification Metadata carrier in `WRITING.METADATA.002`. The block opens and closes with a line containing only `---`. The extension index example now shows that form instead of a bare JSON object.
- Added `templates/docs/aggregate.md` for the `aggregate` specification kind, which the metadata schema, `tools/validate-consumer.mjs`, and `AGENTIC.CONVENTION.002` already required, and listed it in the template index.
- Removed an empty `Vocabulary` heading from the engineering system foundation.
- Added `tools/validate-consumer.cases.mjs` and made it a required CI step. The reference consumer validator previously shipped with no fixture suite and never ran in this repository.
- Built the consumer fixture from the tracked templates, so every shipped Markdown metadata block is now validated as part of the baseline case.
- Made `tools/validate-consumer.mjs` report a metadata block that is not delimited by `---` instead of skipping the file. A silently skipped specification is an unvalidated specification.
- Added seven authoring rules that detect provisions and evidence carrying no information: `PROVISION_RESTATES_HEADING`, `VERIFY_TEMPLATED_EVIDENCE`, `VERIFY_NO_ARTIFACT`, `SUMMARY_RESTATES_REQUIREMENT`, `ID_PREFIX_OWNERSHIP`, `HEADING_EMPTY_BODY`, and `INDEX_CONTAINS_PROCEDURE`.
- Added a warning tier to `tools/validate-standards.mjs`. A code in `WARNING_DIAGNOSTIC_CODES` reports a defect under active repair and does not fail the build. `--warnings` lists every occurrence.
- Removed every version-specific upgrade guide and the requirement to publish future migration instructions. Consumers remain on pinned releases until they select another complete contract.
- Replaced the version-specific adoption guide with `docs/guides/getting-started.md` and removed the duplicate v1 release-scope guide.
- Replaced application-v1 readiness labels with version-neutral release evidence and release-record guidance.
- Removed primary-flow metadata requirements from consumer specifications and the reference consumer validator.
- Removed history-specific authoring checks. The validator evaluates the current standards snapshot.
- Removed archived decision pages and their active navigation. Changelog and Git history retain release context.
- Set the standards manifest version to 1.11.0 without changing schema version 3.

## v1.10.0

- Made `shadcn/ui` with Tailwind CSS v4 the default React web UI system for all product profiles: `public-light`, `application-balanced`, and `admin-dense`.
- Selected Base UI, Vega (`bIkf1RQ`), CSS variables, neutral semantic tokens, Geist, Lucide, the preset radius, and the built-in shadcn registry as the pinned baseline. Added `@base-ui/react`, `next-themes`, and `tw-animate-css`, and refreshed the shadcn, Lucide, Radix compatibility, and slot package pins.
- Amended `UI.GOVERNANCE.001` and `UI.SHADCN.001` and added `UI.TAILWIND.001`, `UI.FORKS.001`, `UI.VOCABULARY.001`, `UI.PAGE.SPEC.001`, `UI.COMPANION.001`, `UI.EVIDENCE.001`, `UI.AGENT.PROTOCOL.001`, and `FTEST.UI.001`.
- Added UI vocabulary, page-contract, and shadcn source-lock schemas and templates. Source locks record generated source digests and require a visible fork classification when source changes.
- Added the controlled React web UI baseline decision and a focused UI override decision template for alternate systems or specialist controls.
- Added the deterministic `tools/validate-ui.mjs` reference validator and made `tools/validate-consumer.mjs` invoke it when a consumer declares a React web platform, a UI configuration, or a `UI.*` override. The validator reads Tailwind rules from extracted class strings rather than whole files, allows Tailwind variant brackets while rejecting arbitrary utility values, classifies top-level global CSS statements, and scans the workspace root and shared packages for a second visual system.
- Added `tools/validate-ui.cases.mjs` with reference passing and failing cases for every UI rule. Its global CSS fixture is the entry the pinned CLI actually generates.
- Required `reviewBy` on every `UI.*` entry in a consumer's `overrides`, and made an override fail once its review date passes so a temporary visual system cannot become permanent by omission.
- Added a vocabulary `runtimeStyles` record as the only route to an inline style for measured geometry, a declared CSS custom property, or a rendering target with no class support such as a generated social image.
- Verified the baseline against the pinned CLI rather than a transcript: `shadcn@4.16.2 preset decode bIkf1RQ` returns the ten recorded values, and a real `init` writes `base-vega`, `rtl: false`, the five recorded aliases, an empty registry map, and exactly the recorded direct dependencies. Two rules were wrong before that check and are now correct: the generated entry imports `tw-animate-css` and `shadcn/tailwind.css` alongside `tailwindcss`, and it expresses its documented browser base rules with `@apply` inside `@layer base`, so `@apply` is prohibited only outside that generated block.
- Pinned `prettier` and `prettier-plugin-tailwindcss`. The source lock normalizes with the project formatter before hashing, so an unpinned formatter would let two consumers compute different digests for identical source.
- Documented that vocabulary and source-lock paths are frontend-relative while `standards.project.json` paths are consumer-root-relative.
- Kept React Native outside the official web baseline and documented separate native platform decisions.
- Consumer action: add frontend UI configuration, vocabulary, source locks, page sidecars, Tailwind restrictions, and the evidence matrix. Existing Radix, Bootstrap, MUI, and other React consumers must record an override and migration plan before moving to the default.

## v1.9.0

- Extended the Domain module-folder convention so a single-aggregate module whose own name equals its aggregate pluralizes the module folder (a `Catalog` aggregate lives in a `Catalogs` folder, namespace `Entro.Domain.Catalogs`) rather than padding the type name to dodge CA1724. Pluralizing the folder is the fix; renaming the type (for example `SalesCatalog`) is not.
- Added a module-identity rule to `NAME.AGGREGATE.001`: a module's identity is one token across the folder and namespace, the `{MODULE}` in failure codes, the `INV-{MODULE}-NN` invariant-id prefix, and the `{module}.{use-case}` id prefix, and the four move as a unit. Renaming only an aggregate leaves them unchanged (`inventory` keeps its token when `CapacityPool` becomes `Capacity`); re-scoping a prefix during a deliberate module rename (`INV-CATALOG-01` to `INV-CATALOGS-01`) preserves the number and is not forbidden renumbering.
- Tightened `DOMAIN.VALUE.001` to a full ban on implicit conversion operators in either direction, for Shared kernel value objects as much as aggregate-owned ones. An implicit primitive-to-value conversion hides the validating factory and lets a cast throw (prohibited by the .NET conversion guidelines); an implicit value-to-primitive conversion erases the domain type. Construct through a named factory and read the value through a named member; an `explicit` operator is permitted only where a boundary needs a cast.
- Added the trailing-doubling case to `NAME.AGGREGATE.001`: when the reason trails with the aggregate noun, drop the repeat (`CapacityInsufficientException`, not `CapacityInsufficientCapacityException`).
- Added a boundary-name convention: route path segments and JSON field names derive from the current ubiquitous term and are renamed with the concept, verified by regenerating the OpenAPI document and typed clients and failing on any diff. Added matching Verification search hints for the implicit-conversion ban, the `Exception`-as-business-term rule, and boundary-name drift.
- Required a boundary string enum to carry the string-enum conversion on the type itself (`[JsonConverter(typeof(JsonStringEnumConverter<T>))]`) in `API.OPENAPI.003`, not only a host-registered converter, so every serializer, including a consumer or test client using default options, honors the contract.
- Extended `DOMAIN.CLOSEDSET.001`: each union exposes a stable code or label whose `FromCode` round-trips for every case, boundaries project the union through that code and never the record's default `ToString()`, and a per-case round-trip test is required. Added the matching Verification check.
- Extended the boundary-name convention: transport field names are the concise business field (`FeeKind`, not `CatalogFeeKind`) matching the use-case specification, and a closed-set code or discriminator string literal is a contract value that an identifier rename must not sweep (the round-trip test catches an altered literal). Added the matching Verification check.
- Added to `BTEST.INTEGRATION.001` that CI executes the integration suite against the container and that a suite which cannot execute fails the build rather than reporting success, so a silently non-running suite cannot hide boundary drift. Added the matching Verification check.
- Added `API.MODELS.001`, which requires a Domain closed set to be mirrored at the transport boundary by a WebApi-owned model of the same shape: a string `enum` for a label-only set and a polymorphic `oneOf` model with a discriminator for a set whose cases carry data. The polymorphic model uses an abstract base record and one sealed record per case, `[JsonPolymorphic]` and `[JsonDerivedType]` carried on the type, string discriminators equal to the Domain union's stable case codes, a concept-named discriminator property rather than the `$type` default, and a document or schema transformer to complete the discriminator when the generator omits it or its required marker. The rule sets consistency over premature narrowing: each layer mirrors the set and no layer serializes a Domain or Application type as the wire contract, and splitting request from response models or reducing a union to an `enum` is a deliberate decision, not the default. Reframed the `API.OPENAPI.003` closed-set bullet so the `enum` is the label-only case and a data-bearing set publishes a `oneOf` per `API.MODELS.001`, and added the matching Verification check.
- Added `APP.CLOSEDSET.001`, which requires a command or query result that carries a Domain closed set to model it with the same shape: a mirrored discriminated union for a data-bearing set (the Domain union directly when it crosses safely, or an Application-owned union) and the stable code for a label-only set, never a Domain-free `enum` reintroduced in Application or a data-bearing union flattened to nullable fields. Added the matching Verification check.
- Extended `DOMAIN.CLOSEDSET.001` so the boundary guidance states the outer layers mirror the union (`APP.CLOSEDSET.001`, `API.MODELS.001`) and an `enum` or string at a boundary is the narrowed representation of a label-only set, not the default for a data-bearing set.
- Added polymorphic transport model naming to `NAME.SUFFIX.001` (`{Concept}Model` base and `{Case}{Concept}Model` cases, dropping the aggregate prefix while the discriminator literal keeps the Domain case code) and a matching Verification check, and added a mirroring boundary to `AGENTS.md`.
- Added `ARCH.CONTRACTS.001`, the contract-level complement to `ARCH.DEPENDENCIES.001`: a layer owns its own messages, results, and transport models and does not expose an inner layer's model outward. An Application message or result exposes no Domain aggregate, closed set, or result record; a WebApi transport model reuses no Application or Domain type. Each layer mirrors the shape it needs, and the duplication is accepted rather than removed with a shared cross-layer DTO or contracts assembly. Shared-kernel typed IDs and value objects (`PostId`, `Money`, `EmailAddress`) are the one sanctioned crossing, an aggregate-owned value object is represented at the Application boundary by its primitive, and the wire contract reduces even the Shared-kernel types to primitives. Tightened `APP.CLOSEDSET.001` so an Application result mirrors a data-bearing set with its own union rather than the Domain union type, refined the Application immutable-messages convention to the same scope, cross-referenced it from `API.MODELS.001`, and added matching Verification checks to `architecture.md`.
- Bumped the manifest version to 1.9.0.

## v1.8.0

- Replaced the module-first Domain folder convention with a top-down `module` then `aggregate` then detail hierarchy, replicated in every layer (`ARCH.MODULES.001` and its mirrored-folders convention). A module with more than one aggregate gives each aggregate its own folder with its own `Events/`, `States/`, and `Exceptions/`, and a closed set groups its base and cases in a folder named for the concept (for example `ScanResults/`) while the aggregate state hierarchy keeps its `States/` folder. A single-aggregate module keeps its aggregate flat. Kind-bucket folders (`Entities/`, `ValueObjects/`, `Services/`) and empty folders remain prohibited. The Application, WebApi, and Infrastructure layer conventions show the same module-then-aggregate nesting.
- Added `NAME.AGGREGATE.001`, which anchors every aggregate-owned type on its aggregate root's full name in first position: events (`OrganizationMemberAccessChangedEvent`), state cases (`PostPublishedState`, aggregate-first), union bases and cases (`RefundOutcome`, `RefundSucceededOutcome`), aggregate value objects (`OrganizationLegalProfile`), child entities, and exceptions. The full aggregate name is used, never an abbreviation, and the prefix chains to the aggregate root. Only Shared kernel types (`Money`, `EmailAddress`) are unprefixed, signaled by their location. The anchor is the aggregate root, not the module (`ReservationNotFoundException`, not `InventoryReservationNotFoundException`), while failure codes stay module-scoped; child-entity identities follow `{Aggregate}{Part}Id`. Domain events now carry the `Event` suffix (`{Aggregate}{PastFact}Event`); an event-reaction handler and its folder keep the `On{PastFact}` form without the suffix. Updated `DOMAIN.STATE.001`, `DOMAIN.CLOSEDSET.001`, `DOMAIN.EVENT.001`, the Domain names table, `NAME.SUFFIX.001`, and the affected examples across the profile and guides.
- Tightened `DOMAIN.EVENT.001` so a domain event carries no `Exception`, `DomainException`, or other error object and is not named after the language error type. A failure that is itself the recorded fact is modeled as immutable domain data (a value object or discriminated-union case) and carried as event data.
- Extended `NAME.EXCEPTION.001` to reserve the `Exception` suffix for `{DomainType}{Reason}Exception` failure types and to prohibit `Exception` as a domain business term for an anomaly or manual-handling case (for example `CancellationExceptionRaisedEvent`), with domain-word alternatives. The exception name anchors on the aggregate root, so an `Event` finance-assurance rule is `EventFinanceAssuranceMislabeledException`.
- Refined `NAME.AGGREGATE.001` from migration experience: a child entity with a first-class domain name (`Reservation`, a child of `CapacityPool`) is itself the anchor for its own states and unions (`ReservationHeldState`, not `CapacityPoolReservationHeldState`); a union case whose concept already embeds the aggregate reorders rather than doubles it (`SuppressionLegalBasis`, `RoleEventScope`); and a per-aggregate folder is named with the plural of the aggregate root so it adds a proper namespace segment (`Entro.Domain.Audience.BuyerAccounts`) that does not collide with the singular aggregate type (which a singular folder would, tripping CA1724).
- Tightened `DOMAIN.REPOSITORY.001` so a required load uses `GetByIdAsync`, which returns the aggregate and throws the aggregate's own `{Aggregate}NotFoundException` (a `DomainException` owning its code, mapped to `404`) from the Infrastructure implementation. A command handler no longer repeats a null check or builds a not-found failure with a hard-coded code and message at the call site. A nullable `FindBy...Async` is reserved for a genuinely optional lookup.
- Bumped the manifest version to 1.8.0 and added the v1.8 consumer upgrade guide.

## v1.7.0

- Added `API.OPENAPI.003`, which requires the generated contract to publish precise, complete schemas: a closed-set field or parameter declares its values as an `enum` (a response field projected from a Domain state hierarchy or discriminated union publishes its allowed values as a typed union rather than an open `string`, with the `enum` living only at the transport boundary per `DOMAIN.CLOSEDSET.001`); a parameter declares its real bounds, format, allowed values, and a description for a non-obvious business limit; and an operation that requires a control header declares it as a required parameter.
- Extended `EXT.CONCURRENCY.IDEMPOTENTOUT.001` guidance so the `Idempotency-Key` header is declared as a required parameter on every operation that requires it, matching `API.OPENAPI.003`, instead of surfacing on only some operations.
- Added `EXT.OUTBOX.READINESS.001`, which separates a dependency outage from a message failure: a Worker that cannot reach its store or finds no schema (cold start or pre-migration) backs off and rate-limits its logging rather than emitting a per-iteration exception storm, and gates its dispatch loop on readiness where the host exposes it. Added a matching cross-reference to `EXT.JOBS.RETRY.001`.
- Bumped the manifest version to 1.7.0 and added the v1.7 consumer upgrade guide.

## v1.6.1

- Fixed the reference consumer validator (`tools/validate-consumer.mjs`) to accept the `implemented` `implementationStatus` value added to the schema in v1.6.0. The schema and the bundled validator now agree, so a specification marked `implemented` passes validation.
- Bumped the manifest version to 1.6.1.

## v1.6.0

- Added `API.OPENAPI.002`, which requires the generated OpenAPI document to reflect enforced authentication: when endpoints enforce authentication (per `API.ACTOR.001`), a document transformer that reads the registered authentication schemes declares the matching `securitySchemes` and per-operation `security`, so a consumer or generated client learns auth is required from the contract rather than from a runtime 401. Intentionally anonymous operations declare no requirement.
- Added the `implemented` value to `implementationStatus` (`planned`, `implemented`, `verified`) in the Specification Metadata schema and defined it in the engineering system: `implemented` records shipped behavior whose acceptance evidence is incomplete, without overstating it as `verified`. It is not sufficient for release; the release standard still requires `verified`. Existing `planned` and `verified` values are unchanged.
- Clarified `UI.SHADCN.001`: the manifest pins the primitive and icon dependencies shadcn/ui source imports (the unified `radix-ui` package and `lucide-react`); adding a component whose source imports an unpinned package requires pinning it first (`DEP.APPROVAL.001`). Either the CLI or hand-authored canonical source may create the owned primitive. Pinned `radix-ui` and `lucide-react` in the manifest.
- Added a frontend rendering note that a `notFound()` thrown in a layout is handled by the parent segment's boundary, not the layout's own segment, with a caveat for route groups and multiple root layouts.
- Added an `API.OPENAPI` convention and a `DATA.TYPES.001` note on numeric transport precision: `int32` and `double` emit plain numeric schemas at the source; the `number | string` union is reserved for `int64`-scale values and is coerced at the consumer read boundary when it legitimately appears.
- Clarified `server-only` guidance: the server-owned module boundary is the baseline; the `server-only` package is an optional import-time guard used only when pinned.
- Added a components convention that card and section titles carry heading semantics rather than a styled `div`.
- Added a profile note on the ESLint 10 and `eslint-plugin-react` interaction and the `settings.react.version` workaround.
- Fixed the page template folder mapping to drop the `src/` segment so it matches `FRONTEND.STRUCTURE.001`.
- Bumped the manifest version to 1.6.0 and added the v1.6 consumer upgrade guide.

## v1.5.0

- Added `DOMAIN.CLOSEDSET.001`, which extends the state-record reasoning of `DOMAIN.STATE.001` to every closed set of Domain values and prohibits declaring any `enum` in Domain. A closed set is modeled as a discriminated union of records (one abstract base, one sealed case per value) or, for a validated scalar with no per-case data, as a typed value object. The rule is scoped to Domain; Application results, transport DTOs, and persistence records at a boundary may still use an `enum` or string and map it to the Domain union.
- Tightened `DOMAIN.ERROR.001` and `NAME.EXCEPTION.001` so each rejected rule has its own exception type that owns its stable failure code and message. A shared exception constructed with a hard-coded `code` and `message` at the call site (for example `RefundRuleException("REFUNDS.ALLOCATION_INVALID", "...")`) is now prohibited; exception constructors accept only the domain values of the specific failure.
- Tightened `DOMAIN.DOCUMENTATION.001` from selected public contracts to every public Domain type and member, with per-member expectations for factories, mutation methods, state and union cases, events, and exceptions.
- Tightened `NAME.FILE.001` to prohibit any file with more than one public or internal top-level type, and named `*Enums.cs` and `*ValueObjects.cs` grouping files as the anti-pattern to split.
- Added `NAME.CSHARP.002`, which requires current language features (collection expressions, `init`/`required` members, `readonly`, primary constructors for dependency-only classes, target-typed `new`, and `switch` expressions over unions) where they are at least as clear as the older construct.
- Added matching baseline boundaries to `AGENTS.md` and Verification steps to the domain and naming conventions, and added the v1.5 consumer upgrade guide.
- Bumped the manifest version to 1.5.0.

## v1.4.1

- Fixed the reference consumer validator (`tools/validate-consumer.mjs`) so it validates structured specifications under `docs/research/`, such as decision-evidence records, instead of skipping the entire directory. Research prose without a metadata block is still skipped, so a decision-evidence record is now checked while Codex or Fable notes are not link-gated.

## v1.4.0

- Added a reference consumer validator at `tools/validate-consumer.mjs` that checks Specification Metadata, internal links, and the cross-file relationships in the foundation Verification lists; wired it into the writing checks, release gates, and consumer verification, and updated the repository stance that previously shipped no bundled validator.
- Extended `AGENTIC.RULES.001` with identifier forms for every rule classification (`VAL-`, `AUTZ-`, `PERS-`, `WFR-` in addition to `INV-` and `POL-`), a `{MODULE}.{REASON}` failure-code convention, and guidance that a decision-derived constraint keeps its enforcement classification and cites the decision rather than minting a `POL-*` ID.
- Defined `docs/operations/` as the home for operating and security reference documents, with cross-cutting security posture, trust boundaries, and threat surfaces recorded as prose (for example `security-and-privacy.md`) rather than in a reintroduced cross-cutting bucket.
- Added a repository configuration convention that negates the `docs/releases/` directory against the case-insensitive `[Rr]eleases/` build-output ignore pattern, with the exact `.gitignore` snippet.
- Stated each extension's activation scope and applicable specification kinds in its own document, in addition to the selection table and manifest.
- Clarified that `WRITING.METADATA.001` forbids any separate Markdown document-metadata section, and that the operating-limits record keeps the fixed `id` `operating-limits` at `docs/operations/limits.md`.
- Bumped the manifest version to 1.4.0 and added the v1.4 consumer upgrade guide.

## v1.3.0

- Defined the Agentic Engineering System as a general specification, engineering, verification, and release model used by Litenova Solutions, with Agent-Driven Engineering as its operating model and Specification-Driven Delivery as its delivery approach.
- Replaced Subject with Module, Business Flow with End-to-End Flow, Primary Business Flow with Primary Release Flow, and Flow Check with End-to-End Test.
- Replaced Follow-up with Event Reaction, Aggregate Rule with Aggregate Invariant, and the Shared Rule umbrella with Domain Policy.
- Replaced Claims and Evidence with Decision Evidence and Release Evidence with Release Record.
- Retained mandatory aggregate state-record hierarchies and prohibited lifecycle enums, status strings, boolean flags, and computed discriminators as replacements.
- Added kind-specific Specification Metadata with `specStatus` and `implementationStatus`.
- Renamed structured kinds, fields, template paths, documentation directories, and `FC-*` identifiers; moved foundation rule IDs to `AGENTIC.*` without compatibility aliases.
- Replaced consumer `extensions` with `selectedExtensions`, added project or local extension activation scopes, and advanced the manifest schema to version 2.
- Replaced the routing schema with a kind-discriminated Specification Metadata schema.
- Simplified the root README, linked the hosted documentation, added project badges and a standards icon, and defined repository writing tone in `AGENTS.md`.
- Added the v1.3 consumer upgrade guide.

## v1.2.0

- Replaced structural capability terminology with subject terminology across ADDD, architecture, templates, and layer conventions.
- Defined a subject as the cross-layer business and navigation boundary that groups one or more related use cases, with one primary aggregate root for state-changing behavior and none for read-only behavior.
- Kept `AggregateRoot<TId>` as the runtime consistency and mutation boundary, and prohibited `Subject`, `ISubject`, and `SubjectRoot` runtime abstractions.
- Replaced the capability specification template with the subject specification template, documented subject and page routing metadata, and added the v1.2 consumer migration guide.
- Replaced `ADDD.CAPABILITY.001` with `ADDD.SUBJECT.001` and `ARCH.CAPABILITIES.001` with `ARCH.SUBJECTS.001`.
- Required command and query results, handlers, and validators to include their architectural role in the type and file name.
- Replaced ambiguous `{Concept}` and `Summary` patterns with `{DomainType}`, `{BusinessTerm}`, `{BusinessRule}`, and `{UseCase}QueryResultItem` patterns.
- Required HTTP transport DTOs and mapping classes to use the explicit `RequestModel`, `ResponseModel`, and `ApiMappings` suffixes.

## v1.1.0

- Added code and documentation consistency checks for business names, capability and use-case paths, acceptance IDs, generated API contracts, duplicate contracts, and removed entry points.
- Added document ownership, authority, freshness, canonical-source, and implementation-evidence requirements for consumer documentation.
- Added product and operating context fields to the product brief for commercial, legal, provider, money, risk, audit, and support constraints.
