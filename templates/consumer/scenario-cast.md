---
{
  "kind": "scenario-cast",
  "id": "__CAST__",
  "specStatus": "approved",
  "owner": "__OWNER__",
  "lastReviewed": "YYYY-MM-DD"
}
---
# __CAST_TITLE__

Every `Scenario` section in this documentation draws its people, place, dates, and amounts from this page. One fixed cast makes those sections cumulative: two pages read in sequence describe one occasion instead of two unrelated illustrations.

This page is informative. It defines no rule, term, state, or actor, and no specification cites it as authority. These names appear in no code, no test, and no stored data.

## The organization

| Fact | Value |
|:---|:---|
| Name | `__ORGANIZATION__` |
| What it does | State its business in one sentence. |
| How often | State the frequency that makes the product worth its price. |
| What it uses today | Name what the organization would use without this product. |

## The people

| Person | Actor | What they do here |
|:---|:---|:---|
| `__PERSON__` | `__ACTOR__` | State the work this person performs. |

Each person maps to one actor from the closed actor set. A scenario names the person, and the actor stays the authority on permission.

## The occasion

| Fact | Value |
|:---|:---|
| Subject | Name the thing the occasion is about. |
| Place | Name where it happens. |
| Date | State the date every scenario reuses. |
| Scale | State the size that makes the occasion ordinary rather than exceptional. |

## The amounts

| Amount | Value | Where it already appears |
|:---|:---|:---|
| `__AMOUNT__` | State the figure. | Name a specification that already uses it. |

Take each amount from figures the specifications already use. A figure invented here contradicts an existing `Examples` section.

## Recurring situations

### __SITUATION__

State one situation that several specifications exist for, and the pressure the people are under while it happens.

## What this page does not do

- It states no rule. A `Scenario` section that disagrees with a specification is the part that is wrong.
- It carries no count, census, or membership list computed from code.
- It changes when the product's subject changes, and not when the code changes.
