{
  "$schema": "../../../standards/schemas/acceptance-criteria.schema.json",
  "schemaVersion": 1,
  "page": "web.page",
  "criteria": [
    {
      "id": "AC-PAGE-01",
      "proves": "The page renders every region its sidecar names.",
      "precondition": "A deployment answering on the address the run opens.",
      "steps": ["Open the route.", "Wait for the page header."],
      "assertions": ["Every region the sidecar names is attached.", "The page has one h1."],
      "spec": "AC-PAGE-01.spec.ts"
    }
  ]
}
