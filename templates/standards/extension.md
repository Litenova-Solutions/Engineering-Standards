# {Extension Title}

## Intent

State the optional capability and its boundary.

## Activation

Activation scope: `{local|project}`.

Applicable specification kinds: `{kind}`.

Activate this extension when one exact current condition applies.

## Baseline relationship

This extension replaces no baseline provision.

## Agent Summary {#agent-summary}

- State one high-risk projection and cite its canonical provision. ({EXT.TOPIC.001})

## Standards

### State one extension action ({EXT.TOPIC.001})

**Requirement:** The activated extension MUST perform one testable action.

**Rationale:** Explain the extension-specific constraint.

**Example:** Show one compliant extension artifact or behavior.

## Conventions

### Use one extension default ({EXT.TOPIC}.CONVENTION.001)

**Default:** State one replaceable extension default.

**Replacement:** Name the local convention or decision that can replace this default.

## Dependencies

None.

## Verification

| ID | Method | Evidence |
|:---|:---|:---|
| {EXT.TOPIC.001} | test | `{Topic}Tests` asserts the exact extension behavior this provision requires. |
| {EXT.TOPIC}.CONVENTION.001 | inspection | Inspect the default or its named local replacement. |
